---
interface Props {
  asChild?: boolean;
  href?: string;
  variant?: "primary" | "secondary" | "default";
  size?: "sm" | "md" | "lg" | "icon";
  class?: string;
  [key: string]: any;
}

const {
  asChild = false,
  href,
  variant = "default",
  size = "md",
  class: className,
  ...rest
} = Astro.props as Props;

const baseClasses =
  "font-medium border rounded-md inline-flex items-center justify-center transition-colors";
const variantClasses = {
  default: "bg-gray-900 dark:bg-gray-100 text-white dark:text-gray-900 hover:bg-gray-800 dark:hover:bg-gray-200 border-gray-900 dark:border-gray-100",
  secondary: "bg-white dark:bg-gray-800 text-gray-900 dark:text-gray-100 hover:bg-gray-50 dark:hover:bg-gray-700 border-gray-300 dark:border-gray-600",
  black: "bg-black text-white hover:bg-gray-800 border-black",
};
const sizeClasses = {
  sm: "px-3 py-1.5 text-sm",
  md: "px-4 py-2 text-sm",
  lg: "px-6 py-3 text-base",
  icon: "p-2",
};

const Tag = href ? "a" : "button";
---

<Tag
  href={href}
  class:list={[
    baseClasses,
    variantClasses[variant === "primary" ? "default" : variant], // map primary to default or handle separately
    sizeClasses[size],
    className,
  ]}
  {...rest}
>
  <slot />
</Tag>
